node_modules/
dist/
coverage/
.env
*.log
.DS_Store

# Embedding cache (regenerated by train-intent-classifier.ts — large, not needed in repo)
scripts/data/_emb_cache.json

# Claude Code local files — machine-specific, may contain tokens
.claude/settings.local.json
.claude/scheduled_tasks.lock
